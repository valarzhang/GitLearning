git branch
git branch -a
git branch -r
git checkout
git checkout -b
git branch -d
git pull
git push

git push origin --delete test_zfl

gitlab中settings->collabrators 添加合作用户

git commit -m


git log


//素质三连
git add .
git commit -m '备注'
git push

git pull --rebase
git rebase --continue

git rebase --abort

//commit 1
git add .

//commit 2
git add .

//commit 1

//commit 2
